{
  "name": "jules-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Jules REST API",
  "type": "module",
  "bin": {
    "jules-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "scripts",
    "README.md",
    ".env.example"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node dist/index.js",
    "check": "node --check index.js && node --check scripts/smoke-test.mjs && node --check scripts/build.mjs",
    "test:smoke": "npm run build && node --env-file=.env scripts/smoke-test.mjs"
  },
  "keywords": [
    "mcp",
    "jules",
    "model-context-protocol"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.76"
  }
}